import { defineAppSetup } from '@slidev/types'
import '../styles/index'

export default defineAppSetup(({ app, router }) => {
  // Place to register global components, directives, etc. if needed later.
})
